import React from 'react';
//import Button from '../../shared/components/FormElements/Button';

const RemoteTaskMenu = props => {
  return (
    <div>
    sms tasks ------ email task
    </div>
  );
};

export default RemoteTaskMenu;
